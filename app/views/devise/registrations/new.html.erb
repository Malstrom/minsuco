<div class="block-center mt-xl wd-xl">
  <!-- START panel-->
  <div class="panel panel-dark panel-flat">
    <div class="panel-heading text-center">
      <a href="#">
        <img src="<%= image_path('angle/logo.png') %>" alt="Image" class="block-center img-rounded" />
      </a>
    </div>
    <div class="panel-body">
      <p class="text-center pv lead">REGISTRATI</p>

      <p class="text-center pv ">Il tuo account è privato, non riveleremo mai i tuoi dati senza un tuo esplicito consenso.</p>
      <%= my_devise_error_messages! %>
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: {class: 'mb-lg'}) do |f| %>
        <div class="form-group has-feedback">
          <%= f.label :email, class: 'text-muted' %>
          <%= f.email_field :email, autofocus: true, class: 'form-control input-lg', id:'email', autocomplete:"off" %>
        </div>
        <div class="form-group has-feedback">
          <%= f.label :password, class: 'text-muted' %>
          <% if @minimum_password_length %>
            <em>(minimo <%= @minimum_password_length %> caratteri)</em>
          <% end %><br />
          <%= f.password_field :password, autocomplete: "off", class: 'form-control input-lg', id:'password', autocomplete:"off" %>
        </div>
        <div class="clearfix">
          <div class="checkbox c-checkbox pull-left mt0">
              <small>
                Cliccando su Crea account, accetti le nostre <a href="#">condizioni</a> e confermi di aver letto la nostra Normativa sui dati,
                inclusa la Normativa sull'uso dei cookie.
              </small>
          </div>
        </div>
        <%= f.submit "ENTRA", class: 'btn btn-block btn-primary mt-lg btn-lg', id: 'sign_up' %>
      <% end %>

      <p class="pt-lg text-center">Hai gia un account?</p><a href="<%= new_user_session_path %>" class="btn btn-block btn-default">
      Fai login
    </a>
    </div>
  </div>
  <!-- END panel-->
</div>