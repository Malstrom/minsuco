it:
  messages:
    join_in_race: "%{who_did} ha partecipato alla gara %{race_name}"
    leave_from_race: "%{who_did} è uscito dalla gara %{race_name}"
    change_status: "%{who_did} ha cambaito il tuo stato in%{status}"
    change_join_value: "%{who_did} ha cambiato il premio per il quale partecipa %{race_name}"
  activerecord:
    errors:
      models:
        event: Eventi
    models:
      event: Eventi
    attributes:
      user:
